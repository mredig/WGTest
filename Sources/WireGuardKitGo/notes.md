env file has minimal env requirements for ios on top. the bottom ones are optional, but can be leveraged for opional config. macos binaries can be built without any env settings provided

build command has an example of running a build
